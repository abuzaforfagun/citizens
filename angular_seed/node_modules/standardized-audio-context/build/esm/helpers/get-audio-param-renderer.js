import { getAudioParamConnections } from '../helpers/get-audio-param-connections';
export function getAudioParamRenderer(anyContext, audioParam) {
    var audioParamConnections = getAudioParamConnections(anyContext, audioParam);
    if (audioParamConnections.renderer === null) {
        throw new Error('Missing the renderer of the given AudioParam in the audio graph.');
    }
    return audioParamConnections.renderer;
}
//# sourceMappingURL=/build/es2015/helpers/get-audio-param-renderer.js.map