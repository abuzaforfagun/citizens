export var disconnectAudioParamInputConnections = function (audioGraph, audioParam, disconnectAudioNodeInputConnections) {
    var audioParamConnections = audioGraph.params.get(audioParam);
    if (audioParamConnections !== undefined) {
        audioParamConnections
            .inputs
            .forEach(function (_a) {
            /*
             * @todo Disconnect the AudioParam.
             * source.disconnect(audioParam);
             */
            var source = _a[0];
            disconnectAudioNodeInputConnections(audioGraph, source);
        });
    }
};
//# sourceMappingURL=/build/es2015/helpers/disconnect-audio-param-input-connections.js.map