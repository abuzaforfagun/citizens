import { IWorkletOptions } from './interfaces';
import { TContext } from './types';
export declare const addAudioWorkletModule: (context: TContext, moduleURL: string, options?: IWorkletOptions) => Promise<void>;
