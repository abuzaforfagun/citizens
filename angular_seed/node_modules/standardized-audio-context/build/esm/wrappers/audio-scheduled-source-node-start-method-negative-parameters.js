export var wrapAudioScheduledSourceNodeStartMethodNegativeParameters = function (nativeAudioScheduledSourceNode) {
    nativeAudioScheduledSourceNode.start = (function (start) {
        return function (when, offset, duration) {
            if (when === void 0) { when = 0; }
            if (offset === void 0) { offset = 0; }
            if ((typeof duration === 'number' && duration < 0) || offset < 0 || when < 0) {
                throw new RangeError("The parameters can't be negative.");
            }
            start.call(nativeAudioScheduledSourceNode, when, offset, duration);
        };
    })(nativeAudioScheduledSourceNode.start);
};
//# sourceMappingURL=/build/es2015/wrappers/audio-scheduled-source-node-start-method-negative-parameters.js.map