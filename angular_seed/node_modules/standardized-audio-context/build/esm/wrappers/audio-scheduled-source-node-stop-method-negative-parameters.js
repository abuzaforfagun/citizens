export var wrapAudioScheduledSourceNodeStopMethodNegativeParameters = function (nativeAudioScheduledSourceNode) {
    nativeAudioScheduledSourceNode.stop = (function (stop) {
        return function (when) {
            if (when === void 0) { when = 0; }
            if (when < 0) {
                throw new RangeError("The parameter can't be negative.");
            }
            stop.call(nativeAudioScheduledSourceNode, when);
        };
    })(nativeAudioScheduledSourceNode.stop);
};
//# sourceMappingURL=/build/es2015/wrappers/audio-scheduled-source-node-stop-method-negative-parameters.js.map